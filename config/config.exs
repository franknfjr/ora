import Config

config :ora, Ora.Repo,
  database: "ora_repo",
  username: "user",
  password: "pass",
  hostname: "localhost"

config :ora, Ora.Repo,
  database: "ora_repo",
  username: "user",
  password: "pass",
  hostname: "localhost"
